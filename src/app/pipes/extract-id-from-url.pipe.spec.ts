import { ExtractIdFromUrlPipe } from './extract-id-from-url.pipe';

describe('ExtractIdFromUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractIdFromUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
