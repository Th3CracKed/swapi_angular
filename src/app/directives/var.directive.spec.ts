import { VarDirective } from './var.directive';

describe('VarDirective', () => {
  it('should create an instance', () => {
    const directive = new VarDirective(undefined, undefined);
    expect(directive).toBeTruthy();
  });
});
